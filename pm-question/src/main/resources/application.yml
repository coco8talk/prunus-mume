spring:
  application:
    name: pm-question
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://${coco8talk.datasource.host:localhost}:3306/${coco8talk.datasource.database:interview_practice_platform}?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: ${coco8talk.datasource.username:root}
    password: ${coco8talk.datasource.password:}
    type: com.alibaba.druid.pool.DruidDataSource
  data:
    redis:
      host: ${coco8talk.redis.host:localhost}
      port: ${coco8talk.redis.port:6379}
      database: ${coco8talk.redis.database:0}

server:
  port: 8083
  servlet:
    context-path: /api

mybatis-plus:
  mapper-locations: classpath*:mapper/*.xml
  type-aliases-package: com.coco8talk.pm.question.model.entity,com.coco8talk.pm.question.bank.model.entity
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      logic-delete-field: deleted
      logic-delete-value: 1
      logic-not-delete-value: 0

sa-token:
  token-name: satoken
  timeout: 2592000
  active-timeout: 1800
  is-concurrent: true
  is-share: true

coco8talk:
  tencent:
    cos:
      secretId: ${TENCENT_COS_SECRET_ID:}
      secretKey: ${TENCENT_COS_SECRET_KEY:}
      region: ${TENCENT_COS_REGION:ap-guangzhou}
      bucketName: ${TENCENT_COS_BUCKET_NAME:}
